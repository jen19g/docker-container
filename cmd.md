# VSCode connect to container of docker

## ① 確保 container 正在跑
### Windows
```sh
docker compose up -d
```

## ② 在 VSCode 按
### Windows
```sh
Ctrl + Shift + P

"輸入指令"
Dev Containers: Attach to Running Container
```

## ③ 選container
